<p>
  <b>Name:</b>
  <%=h @path.name %>
</p>

<p>
  <b>Description:</b>
  <%=h @path.description %>
</p>

<p>
  <b>Edges:</b>
  <% @path.edges.each do |path_edge| %>
    From "<%=h Point.find(path_edge.point_id).name %>" 
	<%=h path_edge.direction %> to 
	"<%=h Point.find(path_edge.endpoint_id).name %>". 
	Polyline: { <%=h path_edge.polyline %> }<br />
  <% end %>
</p>


<%= link_to 'Edit', edit_path_path(@path) %> |
<%= link_to 'Back', paths_path %>
